{
  "name": "x402-circuits",
  "version": "1.0.0",
  "description": "ZK circuits",
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.5"
  },
  "scripts": {
    "build:dir": "mkdir -p build",
    "compile:spend": "circom spend.circom -l node_modules --r1cs --wasm --sym --output build/",
    "compile:credential": "circom credential.circom -l node_modules --r1cs --wasm --sym --output build/",
    "compile": "npm run build:dir && npm run compile:spend && npm run compile:credential",
    "setup:powers": "cd build && npx snarkjs powersoftau new bn128 12 pot12_0000.ptau -v && npx snarkjs powersoftau contribute pot12_0000.ptau pot12_0001.ptau --name='First' -v -e='random' && npx snarkjs powersoftau prepare phase2 pot12_0001.ptau pot12_final.ptau -v",
    "clean": "rm -rf build/*"
  }
}
